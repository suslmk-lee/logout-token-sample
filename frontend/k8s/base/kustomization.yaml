apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: logout-token-frontend-base

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app.kubernetes.io/name: logout-token-frontend
  app.kubernetes.io/component: frontend
  app.kubernetes.io/managed-by: kustomize

images:
  - name: logout-token-frontend
    newName: registry-dev2.k-paas.org/kpaas/logout-token-sample-ui
    newTag: latest