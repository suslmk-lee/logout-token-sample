apiVersion: v1
kind: Service
metadata:
  name: logout-token-service
  namespace: default
  labels:
    app: logout-token-backend
spec:
  type: ClusterIP
  selector:
    app: logout-token-backend
  ports:
  - name: http
    port: 3001 
    targetPort: 3001
    protocol: TCP